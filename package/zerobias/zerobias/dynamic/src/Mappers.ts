/**
 * Mapper for multiple element operations
 * Returns PagedResults<Object> from getChildren, objectSearch, getCollectionElements, searchCollectionElements
 *
 * TODO: Replace with actual mapping logic when schema is defined
 */
export function toMultipleElements(obj: any): any {
  // Placeholder mapping - returns object as-is
  return obj;
}

/**
 * Mapper for single element operations
 * Returns Object from getRootObject, getObject, getCollectionElement
 *
 * TODO: Replace with actual mapping logic when schema is defined
 */
export function toSingleElement(obj: any): any {
  // Placeholder mapping - returns object as-is
  return obj;
}
