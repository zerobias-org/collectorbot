classes:
    # Name of schema artifact that contains the classes below
    - "@auditlogic/schema-auditmation-auditmation-base":
        # List of all classes from the artifact above this collector populates
        - DynamicAccount
        - DynamicGroup
